{
  "name": "@mppt/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "lint": "tsc -p tsconfig.json --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@mppt/contracts": "0.1.0",
    "hono": "^4.9.6",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.35.0"
  }
}
